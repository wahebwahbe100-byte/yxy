const menuBtn=document.querySelector('.menu-btn');const menu=document.querySelector('#menu');if(menuBtn&&menu){menuBtn.addEventListener('click',()=>{const open=menu.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));});}
const forms=document.querySelectorAll('[data-mail-form]');forms.forEach(form=>{form.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(form);const name=data.get('name')||'';const email=data.get('email')||'';const phone=data.get('phone')||'';const service=data.get('service')||'';const message=data.get('message')||'';const subject=encodeURIComponent('Neue Anfrage über die BHV Website');const body=encodeURIComponent(`Name: ${name}\nE-Mail: ${email}\nTelefon: ${phone}\nLeistung: ${service}\n\nNachricht:\n${message}`);window.location.href=`mailto:info@bhv-immobilienverwaltung.de?subject=${subject}&body=${body}`;const status=form.querySelector('.form-status');if(status){status.textContent='Ihr E-Mail-Programm wird geöffnet. Falls nichts passiert, schreiben Sie bitte direkt an info@bhv-immobilienverwaltung.de.';}});});
